import 'package:firebase_core/firebase_core.dart';

/// PLACEHOLDER — replaced by `flutterfire configure`.
///
/// This file only compiles the app; it is never used unless the app is built with
/// `--dart-define=USE_FIREBASE=true`. Run `flutterfire configure` (see
/// docs/FIREBASE_SETUP.md) to generate the real options for your Firebase project.
class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform => throw UnimplementedError(
        'firebase_options.dart is a placeholder. Run `flutterfire configure` '
        'to generate it — see docs/FIREBASE_SETUP.md.',
      );
}
