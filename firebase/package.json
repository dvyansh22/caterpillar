{
  "name": "smart-operator-firebase-seed",
  "version": "1.0.0",
  "private": true,
  "description": "Seed demo operators into Firebase Auth + Firestore for the Smart Operator Assistant.",
  "scripts": {
    "seed": "node seed.js"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0"
  }
}
